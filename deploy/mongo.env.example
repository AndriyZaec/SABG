# Root credentials are used only by MongoDB and the one-shot user initializer.
MONGO_INITDB_ROOT_USERNAME=arena_admin
MONGO_INITDB_ROOT_PASSWORD=REPLACE_MONGO_ROOT_PASSWORD
MONGO_APP_USERNAME=arena_ingest
MONGO_APP_PASSWORD=REPLACE_MONGO_APP_PASSWORD
MONGO_APP_DATABASE=sabg_raw
