# Use localhost for local verification; use <VPS-IP-WITH-DASHES>.sslip.io on Hetzner.
SABG_HOST=localhost
ENVIRONMENT_ACCESS_USER=demo
# Generate with: docker run --rm caddy:2.10.0-alpine caddy hash-password --plaintext 'your-password'
ENVIRONMENT_ACCESS_PASSWORD_HASH=REPLACE_WITH_CADDY_HASH
